


# 🩺 MEDICAL PDF ROUTER
## سیستم مسیریابی و تحلیل اسناد پزشکی با Gemini ADK

این پروژه یک سیستم مسیریابی هوشمند برای اسناد پزشکی (PDF) است که با استفاده از **Agent Development Kit (ADK) Google Gemini** پیاده‌سازی شده است. سیستم ابتدا اسناد را دسته‌بندی کرده و سپس اسناد مربوط به دیابت را به یک مدل تخصصی **یادگیری ماشین (ML)** برای پیش‌بینی‌های دقیق‌تر ارسال می‌کند.

### 💡 معماری و جریان کار

جریان کار از طریق سه ماژول اصلی مدیریت می‌شود:

1.  **Agent روتر اولیه (`medical_pdf_agent`):**
    * تشخیص می‌دهد که $\text{PDF}$ به کدام دسته (دیابت، سرطان عمومی، سرطان سینه یا نامرتبط) تعلق دارد.

2.  **فایل `main.py` (Orchestrator):**
    * خروجی روتر را دریافت می‌کند.
    * اگر دیابت تشخیص داده شد، Agent تخصصی دیابت (`diabetes_analyst_agent`) را با همان فایل $\text{PDF}$ فراخوانی می‌کند.

3.  **Agent تخصصی دیابت (`my_agent`):**
    * با استفاده از $\text{Gemini}$، **۱۴۸ ویژگی** مورد نیاز را از $\text{PDF}$ استخراج می‌کند.
    * آن‌ها را به مدل $\text{ML}$ (بارگذاری شده از `final_diabetes_model.pkl`) ارسال کرده و پیش‌بینی نهایی را برمی‌گرداند.

***

### 📁 ساختار پروژه

MEDICAL_PDF_ROUTER/├── .venv/├── main.py                        # Orchestrator و نقطه شروع برنامه├── requirements.txt               # لیست وابستگی‌ها├── diabetes_sample.pdf            # فایل PDF تستی شما├── medical_pdf_agent/             # پکیج Agent روتر│   └── agent.py└── my_agent/                      # پکیج Agent تخصصی دیابت├── agent.py                   # منطق بارگذاری مدل ML و تعریف Agent استخراج داده└── final_diabetes_model.pkl   # مدل ML فاین‌تون شده
***

### ⚙️ نیازمندی‌ها و راه‌اندازی

#### ۱. تنظیم محیط و وابستگی‌ها

1.  **محیط مجازی** خود را فعال کنید.
2.  فایل `requirements.txt` را ذخیره کرده و وابستگی‌ها را نصب کنید:

    ```bash
    pip install -r requirements.txt
    ```

#### ۲. تنظیم کلید API

کلید $\text{Gemini API}$ خود را به عنوان یک متغیر محیطی تنظیم کنید:

```bash
export GEMINI_API_KEY="YOUR_API_KEY_HERE"
۳. تنظیم مسیر مدل (انجام شده در کد)مسیر مدل $\text{ML}$ در فایل my_agent/agent.py به صورت نسبی تنظیم شده است. مطمئن شوید که فایل final_diabetes_model.pkl در پوشه my_agent قرار دارد.🚀 نحوه‌ی اجرا و تستبرای اجرای کامل جریان کار، فایل $\text{PDF}$ تستی خود را در پوشه ریشه (diabetes_sample.pdf) قرار دهید و main.py را اجرا کنید:Bashpy main.py
خروجی موفقیت‌آمیز:خروجی نهایی باید شامل نتیجه‌ی پیش‌بینی مدل $\text{ML}$ شما باشد:...
🔬 مرحله 2: دیابت تشخیص داده شد. در حال روتینگ به Agent تخصصی دیابت...
...
✨ نتیجه تخصصی دیابت:
مقدار پیش‌بینی شده Fasting Plasma Glucose (mg/dl): [یک مقدار عددی]